"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _ = require(".");

var _bn = _interopRequireDefault(require("bn.js"));

// Copyright 2017-2020 @polkadot/util authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
describe('consts', () => {
  it('BN_ZERO equals 0', () => {
    expect(_.BN_ZERO).toEqual(new _bn.default(0));
  });
  it('BN_ONE equals 1', () => {
    expect(_.BN_ONE).toEqual(new _bn.default(1));
  });
  it('BN_TEN equals 10', () => {
    expect(_.BN_TEN).toEqual(new _bn.default(10));
  });
  it('BN_HUNDRED equals 100', () => {
    expect(_.BN_HUNDRED).toEqual(new _bn.default(100));
  });
  it('BN_THOUSAND equals 1000', () => {
    expect(_.BN_THOUSAND).toEqual(new _bn.default(1000));
  });
});